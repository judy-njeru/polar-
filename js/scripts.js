$(document).ready(function(){
  wow = new WOW(
      {
      boxClass:     'wow',      // default initialises the wow class 
      animateClass: 'animated', // default
      offset:       0,          // default
      mobile:       true,       // default
      live:         true        // default
    }
    )
    wow.init();
}); 
